---
// BookLayout.astro — Main layout for all chapter pages
import '@fontsource/inter/latin.css';
import '@fontsource-variable/noto-sans-thai/index.css';
import '@fontsource-variable/noto-serif-thai/index.css';
import '@fontsource/jetbrains-mono/latin.css';
import '../styles/global.css';
import { getCollection } from 'astro:content';
import { ClientRouter } from 'astro:transitions';
import ChapterNavList from '../components/ChapterNavList.astro';
import EnterpriseHeader from '../components/EnterpriseHeader.astro';
import SearchModal from '../components/SearchModal.astro';
import ThemeHead from '../components/ThemeHead.astro';
import { siteConfig } from '../site.config';
import { absoluteUrl, absoluteUrlFromFullPath, withBase } from '../utils/url';
import { ArrowUp, Clock3, X } from '@lucide/astro';

interface Heading {
  depth: number;
  slug: string;
  text: string;
}

interface Props {
  title: string;
  chapter?: number;
  description?: string;
  readingTime?: string;
  prev?: { href: string; title: string };
  next?: { href: string; title: string };
  headings?: Heading[];
}

const { title, chapter, description = '', readingTime = '10 min', prev, next, headings = [] } = Astro.props;

const allChapters = await getCollection('chapters', ({ data }) => !data.draft);
const chapters = allChapters
  .sort((a, b) => a.data.chapter - b.data.chapter)
  .map((entry) => ({
    num: entry.data.chapter,
    title: entry.data.title,
    href: withBase(entry.id),
    icon: entry.data.icon,
    part: entry.data.part,
  }));

const currentPath = Astro.url.pathname;
const totalChapters = chapters.length;
const currentIndex = chapters.findIndex((c) => c.href === currentPath.replace(/\/$/, '')) + 1;
const progress = Math.round((currentIndex / totalChapters) * 100);

const tocHeadings = headings.filter((h) => h.depth === 2 || h.depth === 3);

const ogImageUrl = siteConfig.ogImage ? absoluteUrl(siteConfig.ogImage, Astro.site) : '';
const canonicalUrl = absoluteUrlFromFullPath(Astro.url.pathname, Astro.site);
---

<!doctype html>
<html lang={siteConfig.lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description || `${title} — ${siteConfig.description}`} />
    <title>{title} — {siteConfig.title}</title>

    <ThemeHead />

    <link rel="canonical" href={canonicalUrl} />

    <!-- Open Graph -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="article" />
    <meta property="og:url" content={canonicalUrl} />
    {
      ogImageUrl && (
        <>
          <meta property="og:image" content={ogImageUrl} />
          <meta name="twitter:card" content="summary_large_image" />
          <meta name="twitter:image" content={ogImageUrl} />
        </>
      )
    }
    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href={withBase('favicon.svg')} />
    <ClientRouter />
  </head>
  <body>
    <a href="#article-content" class="skip-link">Skip to Content</a>
    <div class="reading-progress" id="reading-progress" aria-hidden="true"></div>
    <div
      class="layout grid grid-cols-1 lg:grid-cols-[260px_minmax(0,1fr)] xl:grid-cols-[260px_minmax(0,1fr)_220px] grid-rows-[var(--header-height)_1fr] min-h-screen"
    >
      <EnterpriseHeader showMenu />

      <!-- ═══ MOBILE OVERLAY MENU ════════════════════ -->
      <div
        id="mobile-menu-overlay"
        class="mobile-menu-overlay fixed inset-0 z-[200] lg:hidden"
        role="dialog"
        aria-modal="true"
        aria-label="Book Navigation"
        aria-hidden="true"
        inert
      >
        <div class="mobile-menu-scrim" aria-hidden="true"></div>
        <div class="mobile-menu-panel">
          <div class="mobile-menu-heading">
            <strong>Contents</strong>
            <button id="mobile-close-btn" class="hamburger-btn" aria-label="Close menu">
              <X class="w-5 h-5" />
            </button>
          </div>
          <div class="mobile-menu-scroll">
            <ChapterNavList chapters={chapters} currentPath={currentPath} />
          </div>
        </div>
      </div>

      <!-- ═══ SIDEBAR ══════════════════════════════ -->
      <aside
        class="hidden lg:flex flex-col col-start-1 row-start-2 sticky top-[var(--header-height)] h-[calc(100vh-var(--header-height))] overflow-y-auto border-r border-line-sub bg-surface-sec py-6"
        id="sidebar"
        aria-label="Book Navigation"
      >
        <div class="px-6 mb-6">
          <div class="text-[0.68rem] font-bold uppercase text-content-mut mb-2 flex justify-between">
            <span>Progress</span>
            <span>{currentIndex}/{totalChapters}</span>
          </div>
          <div
            class="h-1 bg-surface-elev overflow-hidden"
            role="progressbar"
            aria-label="Book progress"
            aria-valuenow={progress}
            aria-valuemin="0"
            aria-valuemax="100"
          >
            <div class="h-full bg-[var(--teal-600)] transition-[width] duration-300" style={`width: ${progress}%`}>
            </div>
          </div>
        </div>

        <ChapterNavList chapters={chapters} currentPath={currentPath} />
      </aside>

      <!-- ═══ MAIN CONTENT ═══════════════════════════ -->
      <main
        class="col-start-1 lg:col-start-2 row-start-2 min-w-0 max-w-[100vw] overflow-x-hidden"
        id="main-content"
        data-pagefind-body
      >
        <div class="max-w-[800px] mx-auto px-5 py-10 sm:px-8 lg:px-12 lg:py-14">
          <!-- Chapter Header -->
          <header class="mb-10 border-b border-line-def pb-8">
            {
              chapter !== undefined && (
                <div class="flex items-center gap-2 text-[0.72rem] font-bold text-content-acc uppercase mb-4">
                  <span>Chapter {String(chapter).padStart(2, '0')}</span>
                  <span aria-hidden="true" class="text-content-mut">
                    /
                  </span>
                  <span class="flex items-center gap-1.5 text-content-mut font-medium normal-case">
                    <Clock3 class="size-3.5" /> {readingTime} read
                  </span>
                </div>
              )
            }
            <h1 class="font-display text-[2.55rem] sm:text-[3rem] leading-[1.15] font-semibold text-content-main m-0">
              {title}
            </h1>
            {
              description && (
                <p class="text-[1.05rem] text-content-sec leading-[1.75] max-w-[700px] mt-4 mb-0">{description}</p>
              )
            }
          </header>

          <!-- Article Content -->
          <article class="prose dark:prose-invert max-w-none" id="article-content">
            <slot />
          </article>

          <!-- Chapter Navigation -->
          {
            (prev || next) && (
              <nav
                class={`grid gap-4 mt-16 pt-8 border-t border-line-sub ${prev && next ? 'grid-cols-1 md:grid-cols-2' : 'grid-cols-1'}`}
                aria-label="Chapter Navigation"
              >
                {prev && (
                  <a
                    href={prev.href}
                    id="nav-prev-link"
                    class="flex flex-col p-4 bg-surface-card border border-line-def rounded-md no-underline transition-colors duration-150 hover:bg-surface-hover hover:border-line-str group"
                  >
                    <span class="text-[0.75rem] font-bold uppercase text-content-mut mb-1 transition-colors group-hover:text-content-acc flex items-center gap-1">
                      <kbd class="font-code text-[0.6rem] px-1 py-0.5 border border-line-str rounded bg-surface-base text-content-mut hidden lg:inline-block">
                        ←
                      </kbd>{' '}
                      Previous
                    </span>
                    <span class="font-semibold text-content-main text-lg transition-colors">{prev.title}</span>
                  </a>
                )}
                {next && (
                  <a
                    href={next.href}
                    id="nav-next-link"
                    class="flex flex-col items-end text-right p-4 bg-surface-card border border-line-def rounded-md no-underline transition-colors duration-150 hover:bg-surface-hover hover:border-line-str group"
                  >
                    <span class="text-[0.75rem] font-bold uppercase text-content-mut mb-1 transition-colors group-hover:text-content-acc flex items-center gap-1">
                      Next{' '}
                      <kbd class="font-code text-[0.6rem] px-1 py-0.5 border border-line-str rounded bg-surface-base text-content-mut hidden lg:inline-block">
                        →
                      </kbd>
                    </span>
                    <span class="font-semibold text-content-main text-lg transition-colors">{next.title}</span>
                  </a>
                )}
              </nav>
            )
          }
        </div>
      </main>

      <!-- ═══ RIGHT TOC COLUMN ════════════════════════ -->
      {
        tocHeadings.length > 0 && (
          <aside
            class="hidden xl:flex flex-col col-start-3 row-start-2 pt-10 pr-5 pb-12 sticky top-[var(--header-height)] h-[calc(100vh-var(--header-height))] overflow-y-auto border-l border-line-sub bg-surface-sec"
            aria-label="Table of Contents"
          >
            <div class="px-4">
              <div class="text-[0.72rem] font-bold uppercase text-content-mut mb-4">On this page</div>
              <nav class="flex flex-col gap-2 border-l border-line-sub pl-4 text-[0.85rem]" id="toc-list">
                {tocHeadings.map((h) => (
                  <a href={`#${h.slug}`} class={`toc-item ${h.depth === 3 ? 'toc-item--h3' : ''}`}>
                    {h.text}
                  </a>
                ))}
              </nav>
            </div>
          </aside>
        )
      }
    </div>

    <button
      id="back-to-top"
      class="fixed bottom-5 right-5 z-50 w-10 h-10 flex items-center justify-center rounded-md bg-surface-card border border-line-def text-content-sec opacity-0 pointer-events-none translate-y-2 transition-all duration-150 hover:bg-surface-hover hover:text-content-main"
      aria-label="Back to top"
    >
      <ArrowUp class="w-4 h-4" />
    </button>

    <!-- Copy code button, ScrollSpy & interactions script -->
    <script>
      import { trapFocus } from '../utils/focusTrap';
      import { renderMermaidDiagrams } from '../utils/mermaid';

      document.addEventListener('astro:page-load', () => {
        // ─── 1. COPY CODE BUTTONS (skip ```mermaid blocks — rendered as diagrams below) ───
        // Shiki puts the language on `pre[data-language]`, never as a class on `<code>`.
        document.querySelectorAll('pre').forEach((pre) => {
          if (pre.dataset.language === 'mermaid') return;

          const wrapper = document.createElement('div');
          wrapper.className = 'code-block-wrapper';

          const lang = pre.dataset.language || 'code';

          const header = document.createElement('div');
          header.className = 'code-block-header';
          header.innerHTML = `
          <div class="code-block-lang">
            <span class="code-block-lang-dot"></span>
            <span>${lang}</span>
          </div>
          <button class="code-copy-btn" aria-label="Copy code">Copy</button>
        `;

          pre.parentNode?.insertBefore(wrapper, pre);
          wrapper.appendChild(header);
          wrapper.appendChild(pre);

          const btn = header.querySelector('.code-copy-btn') as HTMLElement | null;
          btn?.addEventListener('click', async () => {
            const code = pre.textContent ?? '';
            await navigator.clipboard.writeText(code);
            if (btn) {
              btn.textContent = '✓ Copied';
              btn.style.color = '#4ade80';
              setTimeout(() => {
                btn.textContent = 'Copy';
                btn.style.color = '';
              }, 2000);
            }
          });
        });

        // ─── 1.5. MERMAID DIAGRAMS ───
        void renderMermaidDiagrams();

        // ─── 2. TOC SCROLLSPY + heading anchor links (headings already have ids) ───
        const article = document.querySelector('#article-content');
        const headingEls = article ? article.querySelectorAll('h2[id], h3[id]') : [];

        headingEls.forEach((heading) => {
          const id = heading.getAttribute('id');
          if (id && !heading.querySelector('.heading-anchor')) {
            const anchor = document.createElement('a');
            anchor.href = `#${id}`;
            anchor.className = 'heading-anchor';
            anchor.setAttribute('aria-label', 'Link to this section');
            anchor.textContent = '#';
            heading.prepend(anchor);
          }
        });

        if (headingEls.length > 0) {
          const observer = new IntersectionObserver(
            (entries) => {
              entries.forEach((entry) => {
                if (entry.isIntersecting) {
                  const id = entry.target.getAttribute('id');
                  document.querySelectorAll('.toc-item').forEach((item) => item.classList.remove('active'));
                  document.querySelector(`.toc-item[href="#${id}"]`)?.classList.add('active');
                }
              });
            },
            { root: null, rootMargin: '-80px 0px -60% 0px', threshold: 0 }
          );

          headingEls.forEach((heading) => observer.observe(heading));
        }

        // ─── 3. MOBILE MENU OVERLAY ───
        const menuBtn = document.getElementById('mobile-menu-btn');
        const mobileCloseBtn = document.getElementById('mobile-close-btn');
        const menuOverlay = document.getElementById('mobile-menu-overlay');

        if (menuBtn && menuOverlay && mobileCloseBtn) {
          let releaseMenuFocusTrap: (() => void) | null = null;

          const openMobileMenu = () => {
            document.body.classList.add('menu-open');
            menuOverlay.classList.add('active');
            menuOverlay.setAttribute('aria-hidden', 'false');
            menuOverlay.removeAttribute('inert');
            releaseMenuFocusTrap = trapFocus(menuOverlay);
            mobileCloseBtn.focus();
          };

          const closeMobileMenu = () => {
            document.body.classList.remove('menu-open');
            menuOverlay.classList.remove('active');
            menuOverlay.setAttribute('aria-hidden', 'true');
            menuOverlay.setAttribute('inert', '');
            releaseMenuFocusTrap?.();
            releaseMenuFocusTrap = null;
            menuBtn.focus();
          };

          menuBtn.addEventListener('click', openMobileMenu);
          mobileCloseBtn.addEventListener('click', closeMobileMenu);

          menuOverlay.addEventListener('keydown', (e) => {
            if (e.key === 'Escape') closeMobileMenu();
          });

          // Close menu when clicking a link
          menuOverlay.querySelectorAll('a').forEach((link) => {
            link.addEventListener('click', closeMobileMenu);
          });
        }

        // ─── 5. SMOOTH SCROLL REVEAL (skipped for prefers-reduced-motion) ───
        // ─── 6. READING PROGRESS BAR + BACK TO TOP ───
        const progressBar = document.getElementById('reading-progress');
        const backToTopBtn = document.getElementById('back-to-top') as HTMLElement | null;
        // @ts-ignore
        if (window.__progressScrollHandler) {
          // @ts-ignore
          window.removeEventListener('scroll', window.__progressScrollHandler);
        }

        if (progressBar || backToTopBtn) {
          // @ts-ignore
          window.__progressScrollHandler = () => {
            const scrollTop = window.scrollY;
            const docHeight = document.documentElement.scrollHeight - window.innerHeight;
            const progress = docHeight > 0 ? (scrollTop / docHeight) * 100 : 0;
            if (progressBar) progressBar.style.width = `${Math.min(progress, 100)}%`;

            if (backToTopBtn) {
              const visible = scrollTop > window.innerHeight;
              backToTopBtn.classList.toggle('opacity-0', !visible);
              backToTopBtn.classList.toggle('pointer-events-none', !visible);
              backToTopBtn.classList.toggle('translate-y-2', !visible);
            }
          };
          // @ts-ignore
          window.addEventListener('scroll', window.__progressScrollHandler, { passive: true });
        }

        backToTopBtn?.addEventListener('click', () => {
          const reduceMotionScroll = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
          window.scrollTo({ top: 0, behavior: reduceMotionScroll ? 'auto' : 'smooth' });
        });

        // ─── 7. KEYBOARD NAVIGATION ───
        // @ts-ignore
        if (window.__keyboardNavHandler) {
          // @ts-ignore
          document.removeEventListener('keydown', window.__keyboardNavHandler);
        }

        // @ts-ignore
        window.__keyboardNavHandler = (e: KeyboardEvent) => {
          // Don't trigger if user is typing in an input or textarea
          if (document.activeElement?.tagName === 'INPUT' || document.activeElement?.tagName === 'TEXTAREA') return;

          if (e.key === 'ArrowRight') {
            const nextLink = document.getElementById('nav-next-link') as HTMLAnchorElement;
            if (nextLink) nextLink.click();
          } else if (e.key === 'ArrowLeft') {
            const prevLink = document.getElementById('nav-prev-link') as HTMLAnchorElement;
            if (prevLink) prevLink.click();
          }
        };
        // @ts-ignore
        document.addEventListener('keydown', window.__keyboardNavHandler);
      });

      document.addEventListener('astro:before-swap', () => {
        const win = window as any;
        if (win.__progressScrollHandler) {
          window.removeEventListener('scroll', win.__progressScrollHandler);
          win.__progressScrollHandler = null;
        }
        if (win.__keyboardNavHandler) {
          document.removeEventListener('keydown', win.__keyboardNavHandler);
          win.__keyboardNavHandler = null;
        }
      });
    </script>
    <SearchModal />
  </body>
</html>
